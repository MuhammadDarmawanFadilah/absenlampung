# PRODUCTION ENVIRONMENT CONFIGURATION - ABSENSI LAMPUNG
# This configuration is used for production deployment

# Database Configuration - Production
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/absenlampung?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Jakarta}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Configuration - Production (Optimized for higher load)
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.read-only=false

# JPA Configuration - Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.defer-datasource-initialization=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Additional Hibernate Configuration for Production
spring.jpa.properties.hibernate.jdbc.batch_size=40
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=false

# Transaction Configuration for Production
spring.transaction.default-timeout=30
spring.transaction.rollback-on-commit-failure=true

# Server Configuration - Production
server.port=${SERVER_PORT:8080}

# Application Timezone
app.timezone=${APP_TIMEZONE:Asia/Jakarta}

# CORS Configuration - Production
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://absenkantor.my.id}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://absenkantor.my.id}

# Jackson Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false

# Multipart Configuration - Production
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=150MB

# File Upload Configuration - Production
app.upload.dir=/opt/absenkantor/uploads
app.upload.max-file-size=10485760

# File Upload Configuration for Photos
app.upload.photos.directory=/opt/absenkantor/uploads/photos
app.upload.photos.max-file-size=10485760
app.upload.photos.allowed-types=image/jpeg,image/jpg,image/png,image/gif

# Wilayah API Configuration
app.wilayah.api.base-url=https://wilayah.id/api
app.wilayah.api.timeout=30000
app.wilayah.api.enabled=true

# Pusher Configuration
pusher.app.id=2013793
pusher.app.key=e3b5f90419b058345c0d
pusher.app.secret=9aba62bcd3966bba24d4
pusher.app.cluster=ap1

# File Upload Configuration for Cuti
file.upload.directory=/opt/absenkantor/uploads/cuti

# Logging Configuration - Production (Less verbose)
logging.level.com.shadcn=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.org.springframework.web.client.RestTemplate=WARN

# Log file configuration - Production
logging.file.name=/opt/absenkantor/logs/application.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.logback.rollingpolicy.file-name-pattern=/opt/absenkantor/logs/application.%d{yyyy-MM-dd}.%i.log
logging.logback.rollingpolicy.max-file-size=50MB
logging.logback.rollingpolicy.max-history=30

# WhatsApp Notification Configuration - Production
whatsapp.api.url=https://tegal.wablas.com
whatsapp.api.token=oHDDXxRgr1XghtF9507vuf79HXFG22ReSBror2QgX2buGFKYNpfCBgY.v6ylljnX
whatsapp.api.sender=6285157661640
whatsapp.api.enabled=true

# Frontend URL - Production
app.frontend.url=${FRONTEND_URL:https://absenkantor.my.id}
frontend.url=${FRONTEND_URL:https://absenkantor.my.id}

# Default Admin User Configuration - Production
app.admin.username=${DEFAULT_ADMIN_USERNAME:admin}
app.admin.password=${DEFAULT_ADMIN_PASSWORD:admin123}
app.admin.email=${DEFAULT_ADMIN_EMAIL:admin@absenkantor.my.id}
app.admin.fullname=${DEFAULT_ADMIN_FULLNAME:Administrator Sistem}
app.admin.phone=${DEFAULT_ADMIN_PHONE:085352073620}

# Security Configuration - Production
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false

# Actuator Configuration - Production (Restricted access)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
management.endpoints.web.base-path=/actuator

# Cache Configuration for Production
spring.cache.type=caffeine
spring.cache.cache-names=provinces,regencies,districts,villages
spring.cache.caffeine.spec=maximumSize=5000,expireAfterWrite=24h

# Security/JWT Configuration for Production
app.security.jwt.secret=${JWT_SECRET:absenkantor-secret-2025}
app.security.jwt.expiration=86400000

# Development Debug Settings for Production
spring.jpa.properties.hibernate.generate_statistics=false

# Data Seeder Configuration - PRODUCTION (NO DUMMY DATA)
app.seeder.insert-dummy-data=no
app.seeder.role.enabled=true
app.seeder.lokasi-kantor.enabled=true
app.seeder.shift.enabled=true
app.seeder.absensi.august-2025=no
app.seeder.absensi.september-2025=no

# Email Configuration (if needed)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# SQL Initialization
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
