# Spring Profile Configuration
spring.profiles.active=local

# Server Configuration
server.port=3001

# Jackson Configuration
spring.jackson.serialization.fail-on-empty-beans=false
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jpa.open-in-view=false

# Multipart Configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=150MB

# File Upload Configuration
app.upload.dir=uploads
app.upload.max-file-size=5MB

# Wilayah API Configuration
app.wilayah.api.base-url=https://wilayah.id/api
app.wilayah.api.timeout=30000
app.wilayah.api.enabled=true

# Pusher Configuration
pusher.app.id=2013793
pusher.app.key=e3b5f90419b058345c0d
pusher.app.secret=9aba62bcd3966bba24d4
pusher.app.cluster=ap1

# File Upload Configuration for Cuti
file.upload.directory=uploads/cuti